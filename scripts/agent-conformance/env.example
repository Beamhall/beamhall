# Beamhall agent-conformance secrets — TEMPLATE (tracked).
#
# The REAL file is `.env` in this directory: generated by provision.sh, chmod 600,
# and gitignored (the repo .gitignore excludes `.env`). bh-mcp-proxy.py reads it,
# looking up the password for its BH_USER. Never put real secrets in this template.
#
# Format: <idp-username>=<password>
admin-alice=CHANGEME
admin-bob=CHANGEME
builder-carol=CHANGEME
builder-dave=CHANGEME
