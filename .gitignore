# Binaries
/beamhalld
/bin/
*.exe
*.test
*.out

# Go
/vendor/
go.work
go.work.sum

# Local data / secrets (appliance runtime state)
/data/
*.db
*.db-wal
*.db-shm
*.age
.env
.env.*

# Maintainer-local docs (status, lab evidence, security reviews, business notes)
/local-docs/
CLAUDE.local.md

# Agent-conformance: machine-local gateway CA (fetched from your appliance)
scripts/agent-conformance/gateway-ca.crt

# Python (agent-conformance tooling)
__pycache__/
*.pyc

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
dist/
/bh-devidp

# Release pipeline: notes extracted from CHANGELOG at release time (release.yml).
# Untracked, must be ignored so GoReleaser's git-clean check does not abort.
.release-notes.md
